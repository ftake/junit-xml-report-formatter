[tool.poetry]
name = "junit xml report formatter"
version = "0.1.0"
description = "Format XML report files generated by JUnit"
authors = ["Fuminobu TAKEYAMA <ftake@geeko.jp>"]
license = "GPL-3.0-or-later"
readme = "README.md"

[tool.poetry.dependencies]
python = "3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
